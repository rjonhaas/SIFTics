# Build output
dist/
**/dist/
node_modules/
**/node_modules/

# Per-case run output. Per CLAUDE.md these are gitignored except for one
# example checked in deliberately. Keep working scratch out of the repo.
analysis/
orchestrator/analysis/
orchestrator/-/

# Working directory used for evidence working copies and tool artifacts.
# Multi-GB; never belongs in git.
working/

# Per-incident scratch files written by scripts/run-incident.sh
.directive-*.json
.preflight-*.json

# Per-tool ad-hoc output not under analysis/
*.log
!**/SKILL.md
!**/README.md
!**/example*.log
